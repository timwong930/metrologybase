{
  "name": "metrologybase",
  "version": "1.1.0",
  "private": true,
  "description": "MetrologyBase — practical, independent metrology education and tools.",
  "scripts": {
    "dev": "npx serve .",
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css site.css app.js dist/ && cp -R learn tools glossary about dist/ && cp -R public/. dist/"
  }
}
