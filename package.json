{
  "name": "metrologybase",
  "version": "1.0.0",
  "private": true,
  "description": "MetrologyBase — practical, independent metrology education and tools.",
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'Static site — no build required'"
  }
}
