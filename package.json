{
  "name": "metrologybase",
  "version": "1.0.0",
  "private": true,
  "description": "MetrologyBase — practical, independent metrology education and tools.",
  "scripts": {
    "dev": "npx serve .",
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css app.js dist/ && cp -R public/. dist/"
  }
}
